//The Measurable interface type requires a single method, getMeasure.
// In general, an inter­ face type can require multiple methods.

public interface Measurable{

    // method in the interface are default public, no need to declare public
    double getMeasure();
}